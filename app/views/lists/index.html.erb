<h1>C l'index</h1>
<% @lists.each do |l|%>
<%= link_to l.name, list_path(l)%>
<%= link_to "X", list_path(l), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<% end%>
<%= link_to "Create a list", new_list_path, class:"btn btn-flat" %>
